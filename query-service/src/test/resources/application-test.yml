spring:
  datasource:
    url: ${REPLACED}
    username: ${REPLACED}
    password: ${REPLACED}
  kafka:
    bootstrap-servers: ${REPLACED}

logging:
  level:
    root: INFO
    com.mayosen.financeapp: TRACE
    org.springframework.jdbc.core.JdbcTemplate: DEBUG
    org.springframework.kafka: DEBUG
    org.apache.kafka: WARN
    org.awaitility: TRACE
